{
  "name": "anime_searcher",
  "version": "1.0.0",
  "description": "searches an API for anime and allows you to bookmark them",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Lewis",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.15.4"
  },
  "dependencies": {
    "core-js": "^3.44.0",
    "regenerator-runtime": "^0.14.1"
  }
}
